import { StyleSheet, Dimensions } from 'react-native';

const heightWindow = Dimensions.get('window').height * 0.82;

export const styles = StyleSheet.create({
    wrapperForm: {
        height: '100%'
    }
});

